# Pass L1 plan

- Preserve `/` byte-for-byte and build `/landing-next` as an unlinked client-only route.
- Reuse the current landing’s first two beats, marketing pieces, loop mark, spider, library section, privacy demo, close, and footer.
- Add additive `landing.viewed` dimensions for `variant` and `surface`, with no event-name or consent changes.
- Add token-based scroll timeline styles and focused copy/variant tests.
- Verify only the new tests plus desktop and mobile rendering.
