# Work screen pass 2

## Scope
- Add the supplied short date helper and connected-tools subtitle.
- Extend the shared page header with the supplied optional action slot.
- Apply the short date only to dense work cards.
- Apply the supplied `/work` header action, filter chip, collapsed add-work row, and footer panel layout.
- Update the `/work` DESIGN-PORT entry to BUILT for pass 2.

## Technical constraints
- Preserve all existing hooks, reads, handlers, controls, telemetry, consent behavior, routes, and route parameters except the explicitly supplied additions.
- Do not alter the landing page or any file it renders.
- Run type checking and the full test suite; report relevant pinned-test failures without rewriting them.
- Do not deploy or mark anything VERIFIED.
