import { PageHeader } from "@/components/layout/PageHeader";

export function WorkPage() {
  return (
    <PageHeader
      title="All work & mapping"
      subtitle="Everything you've connected, mapped to engagements and tasks."
    />
  );
}