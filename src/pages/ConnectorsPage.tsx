import { PageHeader } from "@/components/layout/PageHeader";

export function ConnectorsPage() {
  return (
    <PageHeader
      title="Where work lives"
      subtitle="Connect the places your work already happens."
    />
  );
}